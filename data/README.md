# data 目录

每周上传新的卷烟货源投放策略表 xlsx 文件到本目录。

## 命名建议

文件名建议包含日期，便于追溯，例如：

```
全区卷烟货源投放策略表（2026年6月21日下午-2026年6月26日上午）.xlsx
```

## 自动触发

当本目录下的 `.xlsx` 文件发生变更（新增、修改、删除）并推送到 GitHub 时，
会自动触发 `.github/workflows/analyze-cigarettes.yml` 工作流，
解析后生成的 JSON 会提交到 `output/` 目录。
